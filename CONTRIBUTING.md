# Development

Run `mise run setup` and `mise run check`. Keep scientific claims task-conditional, protect evaluator-side state, preserve immutable answers and validate parameter recovery after experimental changes. Report hashes bind exact bytes. Never commit credentials, databases, private reports or generated dependencies.

# Clarified source delivery and passport acceptance — 26 September 2026

The [0.2 collection interface](source-delivery-v2.md) and [source-learning passport](source-learning-passport.md) now work through the complete local path. One fresh Astra respondent completed the clarified MCP collection, its original records reconstructed into a passport, and that passport passed offline demonstration signing and verification. The existing panel also reconstructs into explicit configuration-level profiles. This is transport, derivation and interface acceptance; verification benefit remains untested.

## Version and synthetic checks

The source generator, inference engine and answer sequence are unchanged. New wording, research definitions and final-outcome receipts live in the separately fingerprinted `epistemics.source_delivery` module. Original source-learning and source-panel fingerprints remain unchanged, and no existing human session was edited.

| Validation seed | Datasets | Primary-pair accuracy | Joint gain MAE | Joint later latent RMSE, points | Gates |
| --- | ---: | ---: | ---: | ---: | --- |
| 20260925 | 96 | 100% | 0.02719 | 0.36899 | Pass |
| 20261025 | 96 | 100% | 0.03000 | 0.38640 | Pass |

These reuse the original engine's declared seeds and recovery gates: at least 90% primary-pair accuracy, at most 0.15 joint gain MAE and at most five-point joint latent prediction RMSE. Every implemented generating family won its 32 replicates under each seed. Clarified language has no behavioral effect in this simulator; these checks establish engine continuity, not that wording helps a real participant.

Tests cover complete browser HTTP and stdio MCP collections, all research branches, sparse/dense and structured/packet delivery, private-state boundaries, identical retry receipts, concurrent submission, exact old fingerprints, delivery tampering, report reconstruction, mixed-condition rejection, repeated-session derivation, missing repeatability and cohort identity. Both new schema snapshots match their Pydantic contracts. The main `report.v1` contract is unchanged.

## Fresh respondent

A private plan, complete implementation snapshot, new world, exact configuration and operator adapter were frozen before the single respondent attempt. The requested model was `gpt-6-astra`, medium reasoning, in a fresh ephemeral context with only the five public collection tools. The model alias, execution and context isolation remain operator assertions. The limit was one attempt of at most 900 seconds, with no automatic retry.

| Observation | Result |
| --- | --- |
| Collection | Sparse, structured; 24 companies / 36 checkpoints |
| Duration | 179.263 seconds — 2 minutes 59 seconds |
| Accepted / rejected / repeated submissions | 36 / 0 / 0 |
| Public MCP calls | 76 |
| Postcompletion comprehension | 4 of 4 correct on operator semantic review |
| Decisions consistent with reports and thresholds | 36 of 36 |
| Research | 2 customer panels, 1 selection audit, 1 measurement audit, 8 stops |
| Final-outcome Brier | 0.101242 over 24 companies |
| Realized fictional payoff | 3.41 total; 0.142083 per company, including research costs |
| Frozen fitted later-report RMSE | 6.1533 probability points |
| Fixed joint observer RMSE | 4.6664 probability points |
| Within-session adequacy screen | All candidate families outside their conditional envelopes |

The respondent correctly stated that demand is revealed after the final answer, source audits apply to later reports, accurate measurement does not imply random selection, and an independent customer sample does not reveal demand with certainty. It described the instructions as clear and identified long repeated archives and research descriptions as remaining friction. Its report is feedback from one agent, not human usability evidence.

The fixed observer again predicted better than individual fitting; joint-process fit error exceeded its 4.3771-point conditional 99% simulation boundary. The passport exposes this model inadequacy. The outcome score does not contradict the prediction failure: forecasting outcomes and forecasting the respondent's numerical reports are different objectives. This new world is not a matched wording comparison and cannot show an improvement caused by clarification. It also supplies no repeated-session estimate.

Recorded usage: 1,406,285 input tokens, including 1,362,816 cached input tokens, plus 3,744 output tokens; 1,410,029 processed tokens in total. Account marginal dollar cost is unknown. The CLI emitted its known under-development feature warning; no MCP calls failed. Initial operator preparation failed on an unsupported argument before any collection or respondent launch. That preparation and its plan were preserved; a corrected plan preceded the sole respondent attempt. No answers were discarded or substituted.

## Passport reconstruction and machine use

Original panel files were hashed before and after derivation and remained byte-identical. Each baseline aggregate contains four sessions, two generated worlds, 96 company evaluations, 144 checkpoints and 48 research choices. Repeat RMS uses two matched pairs of twelve unaudited forecasts each.

| Existing baseline aggregate | Repeat RMS, points | Final Brier | Mean fictional payoff/company |
| --- | ---: | ---: | ---: |
| Astra | 2.85044 | 0.124683 | 0.160417 |
| Sol | 10.80123 | 0.148433 | 0.125885 |

These are re-expressions of [the completed panel](source-panel-results-2026-09-26.md), not new observations. Different session subject IDs remain visible, and each aggregate is labeled a **configuration cohort**. The signer refuses to issue such an aggregate as one agent identity. The original Astra packet session also reconstructs successfully and remains scoped to packet presentation.

The fresh clarified run produces a separate single-subject passport with repeatability absent. Its exact source passed Python derivation verification, followed by the TypeScript offline demonstration signer/verifier using an ephemeral key. The key was discarded; this does not establish issuer trust, registry association or independently verified execution. The readable artifact remains a draft with a detached demonstration envelope.

Seventeen source metric IDs are available. Only single-subject sparse structured evidence is qualified for the initial policy mapping. The tests exercise local HTTP retrieval and **mocked RPC**: a provisional policy can use recorded facts; requiring missing repeatability or predictive validation returns review; synthetic evidence is ineligible. No payment is authorized. Cohort, dense, packet and unknown-delivery scopes cannot silently qualify.

Repository validation covered 334 Python tests. The initial full run passed 333 and caught an error-message compatibility assertion; its correction passed all 17 tests in that module. Final passport changes passed the affected 29 tests. Ruff and formatting checks passed. TypeScript compilation, Biome and all 40 TypeScript tests passed. The generated aggregate HTML was visually inspected; actual human completion remains pending.

Private artifacts are under `output/source-delivery-agent-20260926b/` and `output/source-passports-reviewed-20260926/`. Raw worlds, plans, responses and source bundles are excluded from Git; only aggregate results and byte commitments are recorded here.

## Artifact commitments

| Artifact | SHA-256 |
| --- | --- |
| Clarified delivery implementation | `b45e472082a50b3bda49cfae04147e3622f5df5ea64274d5b6710391825d2bb0` |
| Validation A | `a1fbad241254c151ae92bc1c4456e84f09b20f212033c4b99dc979049e61e238` |
| Validation B | `1329e407c3f2c23728b5ff295dd4acc0baa5089843fba4d6008655f56fa2b208` |
| Corrected precollection plan | `84c274faf45109898a1a69dc790ef95222c4cde2ac7403972c84f951f4913628` |
| Completed canonical report | `a14b8fd2f92d2c7e3f3674adf581add92d13fc5a09140ef2be285213ff8e88ee` |
| Completed delivery evidence | `434d5474948ee3412cf91c7e3137be46bfe150f47065051528bd51ee039c61a7` |

These are operator-held exact-byte commitments, not independent timestamps or execution attestations.

## Next increment

Implement the separately versioned assigned-check collector and prospective policy locks described in [the verification-value protocol](verification-value-v1.md), then validate it and freeze an actual run plan. The proposed first comparison is eight fresh contexts testing no check against always buying a fallible independent sample. Only a useful general check justifies testing whether passport-guided allocation adds value over a simpler policy. No verification experiment, buyer outreach, registry write, paid endpoint or payment was performed in this increment.

# Evaluator-local state; never share the live database with evaluated agents.
EPISTEMICS_DB=.epistemics/evaluations.sqlite3
# Devnet-only CLI submission. No key or funded account is needed for offline use.
SOLANA_RPC_URL=https://api.devnet.solana.com
SOLANA_KEYPAIR_PATH=
